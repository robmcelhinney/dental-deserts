# Success Criteria

- Postcode search recenters the map reliably and returns nearby practices.
- Initial app render remains under 2 seconds on normal broadband for the MVP dataset.
- Reported-availability caveats appear in UI and docs.
- Data rebuild is reproducible via a single command: `make build-data`.
